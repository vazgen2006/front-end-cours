    /** 
     * JS data types
     * (tvyalneri tiper)
    */
    
    //Dinamic(tuyl) & Static(hastatun)

    // 1 Primitive types
    // 2 nunber (..., NaN, Infinity, -Infinity)
    // 3 string
    // 4 boolean(true, false)
    // 5 undefined
    //  null
    // +-

    "use strict"

    /**
     * Operators
     */

     // 1. unar
     // 2. binar (+, -, *, /, %) (= +=, -=, *=, /=, %=,), (>, <, >=, <=, ==, !=, ===, =!), (||, &&), (|, &)
     // 3. ternar
    
    var d = 10

     d = d + 5

    // && (true && true) = true
    // && (true && false) = false
    // && (false && true) = false
    // && (false && false) = false


    // || (true || true) = true
    // || (true || false) = true
    // || (false || true) = true
    // || (false || false) = false